services:
  php:
    image: REGISTRY_CHANGE_ME/IMAGE_NAME_CHANGE_ME:TAG_NAME_CHANGE_ME
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./:/var/www/html:delegated
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: on-failure